connectorDetails:
  - type: "firstDataSource" 
    queryDetails: 
      - name: "USER"
        query: "SELECT * FROM USERS"
      - name: "CLIENT_BANKS"
        query: "SELECT * FROM CLIENT_BANKS"
  - type: "secondDataSource" 
    queryDetails: 
      - name: "USER"
        query: "SELECT * FROM USERS"
      
      
    